<div class="select-container">
	<div class="row">
		<h2 class="col-md-6 col-xs-6">Select Owner:</h2>
		<h2 class="new-form-button btn btn-info col-md-3 col-md-offset-3 col-xs-3 col-xs-offset-3">New Owner</h2>
	</div>
	<div class="row new-form col-md-5 col-md-offset-7 col-xs-5 col-xs-offset-7">
		<%= render partial: "new", locals: {owner: @owner} %>
	</div>
	<select class="owner-select form-control">
		<% @owners.each do |owner| %>
			<option value="<%=owner.id%>"><%=owner.name%></option>
			<%end%>
		</select>
	<div class="business-links">
	</div>
</div>